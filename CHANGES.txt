УБРАН ОРАНЖЕВЫЙ БАННЕР — что внутри архива

ИЗМЕНЕНО:
  src/app/layout.tsx — убран <AdBanner /> (тот самый оранжевый блок сверху, во всю ширину,
                        с крестиком) и его импорт

Это была отдельная третья реклама на сайте (кроме большой карусели и карточки в сайдбаре) —
компонент src/components/AdBanner.tsx остался в проекте нетронутым (на случай, если он снова
понадобится), просто убран из layout.tsx, так что нигде на сайте больше не показывается.

Применить: распаковать поверх проекта, как и раньше.
