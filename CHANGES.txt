БОЛЬШОЙ ПРОМО-БАННЕР — что внутри архива

ИЗМЕНЕНО:
  src/components/PromoCarousel.tsx  — плавный слайд вместо мгновенной смены картинки,
                                       автопрокрутка теперь каждые 5 сек (было 6), свайп
                                       пальцем на телефоне, драг мышью на десктопе, стрелки
                                       влево/вправо (на десктопе появляются при наведении)
  src/app/page.tsx                  — баннер перенесён в самый верх страницы (теперь первое,
                                       что видно, до шапки-хиро), блок "реклама + пополнение"
                                       остался под шапкой, но уже без баннера — просто две
                                       карточки рядом

Применить: распаковать поверх проекта, как и раньше.

Примечание: автопрокрутка и стрелки/свайп появляются только если в баннере больше одного
рекламного слайда (настраивается в админке). Если слайд один — карусель просто показывает
его статично, это ожидаемо.
